import { NavLink } from "react-router-dom";

const NotFoundPage = () => {
  return (
    <div>
      <h2>NotFoundPage</h2>

      <NavLink to="/">Go back</NavLink>
    </div>
  );
};

export default NotFoundPage;
